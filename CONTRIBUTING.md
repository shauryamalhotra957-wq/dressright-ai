# Contributing

Install the locked JavaScript dependencies and run the project checks:

~~~bash
npm ci
npm test
npm run build
~~~

Keep recommendations explainable and avoid storing sensitive body measurements or images without explicit consent. Add tests for validation, fallback behavior, and accessible UI states when changing the recommendation flow.

Do not commit user photos, credentials, or generated private profiles.
